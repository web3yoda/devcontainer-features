{
    "name": "useful-utils",
    "id": "useful-utils",
    "version": "1.1.5",
    "description": "A collection of useful-utils",
    "options": {
        "yq_version": {
            "type": "string",
            "enum": [
                "latest"
            ],
            "default": "latest",
            "description": "yq version"
        },
        "dasel_version": {
            "type": "string",
            "enum": [
                "latest"
            ],
            "default": "latest",
            "description": "dasel version"
        },
        "gum_version": {
            "type": "string",
            "enum": [
                "latest"
            ],
            "default": "latest",
            "description": "gum version"
        },
        "gomplate_version": {
            "type": "string",
            "enum": [
                "latest"
            ],
            "default": "latest",
            "description": "gomplate version"
        },
        "task_version": {
            "type": "string",
            "enum": [
                "latest"
            ],
            "default": "latest",
            "description": "task version"
        },
        "w3t_version": {
            "type": "string",
            "enum": [
                "latest"
            ],
            "default": "latest",
            "description": "w3t version"
        },
        "supervisord_version": {
            "type": "string",
            "enum": [
                "latest"
            ],
            "default": "latest",
            "description": "supervisord version"
        },
        "session_manager_plugin_version": {
            "type": "string",
            "enum": [
                "latest"
            ],
            "default": "latest",
            "description": "session-manager-plugin version"
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
