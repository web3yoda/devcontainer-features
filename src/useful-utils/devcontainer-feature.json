{
    "name": "useful-utils",
    "id": "useful-utils",
    "version": "1.1.1",
    "description": "A collection of useful-utils",
    "options": {
        "yq_version": {
            "type": "string",
            "enum": [
                "latest"
            ],
            "default": "latest",
            "description": "yq version"
        },
        "gum_version": {
            "type": "string",
            "enum": [
                "latest"
            ],
            "default": "latest",
            "description": "gum version"
        },
        "gomplate_version": {
            "type": "string",
            "enum": [
                "latest"
            ],
            "default": "latest",
            "description": "gomplate version"
        },
        "session_manager_plugin_version": {
            "type": "string",
            "enum": [
                "latest"
            ],
            "default": "latest",
            "description": "session-manager-plugin version"
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
